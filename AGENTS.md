<!-- convex-ai-start -->

This project uses [Convex](https://convex.dev) as its backend.

When working on Convex code, **always read
`convex/_generated/ai/guidelines.md` first** for important guidelines on
how to correctly use Convex APIs and patterns. The file contains rules that
override what you may have learned about Convex from training data.

Convex agent skills for common tasks can be installed by running
`npx convex ai-files install`.

<!-- convex-ai-end -->

# Project context

Compari is currently a Convex + TanStack Start starter application. The
visible UI is still the starter experience: it displays persisted numbers,
adds random numbers, and demonstrates a Convex action on a second page.

## Stack and structure

- React 19 with TanStack Start, TanStack Router, TanStack React Query, Vite,
  and Tailwind CSS.
- Convex provides the backend and database. `src/router.tsx` connects the
  `ConvexQueryClient` and wraps the app with `ConvexProvider` using
  `VITE_CONVEX_URL`.
- `src/routes/index.tsx` is the home page and uses the `numbers` query and
  `addNumber` mutation.
- `src/routes/anotherPage.tsx` demonstrates calling `myAction`.
- `convex/schema.ts` defines the `numbers` table with a numeric `value`.
- `convex/myFunctions.ts` contains the public `listNumbers` query,
  `addNumber` mutation, and `myAction` action.
- `convex/_generated/` is generated by Convex and should not be edited by
  hand.

There is no checked-in deployment configuration, Convex component
registration, or authentication provider configuration yet. Treat deployment
URLs, credentials, and environment variable values as private; do not add
them to source or documentation.

## Local development

Run these commands from the project root:

- `npm run dev` starts Convex development and Vite together.
- `npm run typecheck` runs TypeScript without emitting files.
- `npm run lint` runs typechecking and ESLint.
- `npm run build` typechecks and builds the Vite app.
- `npm run format` formats the project with Prettier.

When changing files under `convex/`, read
`convex/_generated/ai/guidelines.md` before making changes and follow its
validator, function-registration, and schema rules.

## Commit often

Commit frequently whenever a small, coherent unit of work is complete. Keep
commits focused and use Conventional Commits syntax:

```text
<type>(<optional-scope>): <concise imperative description>
```

Common types are `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `build`,
`ci`, `perf`, and `style`. Examples:

```text
feat(numbers): add persisted comparison results
fix(convex): validate empty input
docs: document local development
```

Before committing, review the diff and run the most relevant checks, usually
`npm run typecheck` and `npm run lint`. Do not combine unrelated changes in a
single commit.
